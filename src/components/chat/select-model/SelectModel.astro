---

---

<form class="form_model_select">
    <label
        >Escoge un modelo:
        <select
            class="form-select model_selector"
            id="model_selector"
            name="model_selector"
            aria-label="Default select example"
        >
            <option value="tiny" selected>TinyLLama - 839.98 VRAM</option>
            <option value="gemma">Gemma - 1750.66 VRMA</option>
            <option value="stable">Stable - 2999.33 VRAM</option>
        </select>
    </label>
    <button type="submit">Seleccionar</button>
</form>
<style>
    .form_model_select {
        /* border: 1px solid; */
        width: 100%;
        height: min-content;
        display: flex;
        align-items: flex-end;
        justify-content: space-evenly;
        > label {
            /* border: 1px solid; */
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            align-items: flex-start;
            gap: 8px;
            font-weight: bold;
            > .model_selector {
                width: 100%;
                height: 40px;
                font-size: 0.9rem;
            }
        }
        > button {
            border: 0;
            height: 40px;
            border-radius: 5px;
            padding: 5px 8px;
            background-color: rgba(68, 136, 238, 0.813);
            color: #fff;
            cursor: pointer;
        }
    }
</style>
